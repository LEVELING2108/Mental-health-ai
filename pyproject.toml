[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q --cov=api --cov=model --cov=core"
testpaths = [
    "tests",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]
ignore = ["E501"]
